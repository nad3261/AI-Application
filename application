import streamlit as st
from model import transformer_model  # Your model script

st.title("Transformer Model App")

input_text = st.text_input("Enter input text:")

if st.button("Run Model"):
    output = transformer_model(input_text)
    st.write(f"Model Output: {output}")
